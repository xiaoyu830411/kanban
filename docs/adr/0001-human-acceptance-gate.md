# 人工验收门：Agent 不能将任务移入「已完成」

Agent 只能将任务推进到「待验收」；移入「已完成」（验收）或退回，是成员独有的动作。这把「Agent 声称做完」和「人确认做完」分成两个不可混淆的状态，是企业场景下信任模型的锚点。协议在 API 层强制执行，而非提示词约定。该模式由 dsh-taskboard 验证有效。

## Considered Options

- 允许 Agent 完成任务（全自动闭环）——被否：无人把关的任务质量不可信
- 提示词层面约束——被否：协议必须是代码强制的，不能靠约定
