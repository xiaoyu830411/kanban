# Agent 任务看板（Agent Task Board）

企业内部的人机协作任务看板：成员手工创建任务或由 Agent 创建任务，Agent 认领并执行，成员人工验收。v1 为单组织部署。

## Language

### 组织与人员

**组织（Organization）**:
整个系统服务的单一企业边界，v1 每个部署只有一个组织；成员经飞书扫码登录加入。
_Avoid_: 公司、租户、团队

**成员（Member）**:
组织中注册的人，通过账号登录使用看板。v1 只有两种组织角色：管理员、成员。
_Avoid_: 用户、员工

### 工作空间与执行者

**工作空间（Workspace）**:
看板与 Agent 的容器。v1 每个成员只有一个自动创建的「我的空间」；共享空间是后续版本的概念。
_Avoid_: 项目、团队、分组

**我的空间（My Space）**:
成员登录后自动获得的个人工作空间，是其任务与 Agent 的默认归属地。
_Avoid_: Default 空间、个人看板

**Agent**:
由成员创建并拥有的自动化执行者，持有一个 API token，代表一类执行程序（如 claude-code）；在其所属空间内认领并执行任务。
_Avoid_: 机器人、worker、session

**认领（Claim）**:
Agent 把任务转入「进行中」并独占持有的动作；同一时间一个任务只能被一个 Agent 持有。
_Avoid_: 领取、接单

**指派（Assign）**:
在任务上可选指定一个 Agent；指派后仅该 Agent 可认领此任务，未指派则空间内任何 Agent 均可认领。
_Avoid_: 分配、allocate

**执行（Run）**:
Agent 承担一个任务的完整周期：自认领起，至提交执行报告并将任务移入待验收止；同一时间一个任务至多有一次执行。
_Avoid_: session、进程、实例

**释放（Release）**:
放弃持有、使进行中的任务回到待办的动作；Agent 可自行释放，成员可强制释放，与认领互逆。区别于「退回」（验收不通过回到进行中，持有不变）。
_Avoid_: 退回、取消、unclaim

**启动（Launch）**:
成员在待办任务上发起一次执行的动作；执行由成员本机的 Agent 侧软件承担。启动即认领：任务转入进行中并获得持有。
_Avoid_: 运行、触发

### 任务

**任务（Task）**:
看板上的工作单元，可由成员手工创建，也可由 Agent 通过 API 创建；可指派给一个 Agent。由 Agent 认领执行，由成员验收关闭。
_Avoid_: 工单、卡片、issue

**执行目录（Execution Directory）**:
任务创建时指定的执行地点，三选一：临时目录（默认系统 /tmp，可配置）、指定目录（执行时须已存在）、Git 仓库（本地路径或远端地址，执行时检出）。Git 型可另附起始分支。
_Avoid_: 工作目录、workspace

**起始分支（Start Ref）**:
Git 型执行目录可选指定的检出起点：执行分支自该分支（而非仓库默认 HEAD）创建。执行分支名固定为 task/<任务号>，不受起始分支影响；改起始分支只在执行现场干净时可重新检出。
_Avoid_: 分支名、base branch、checkout

**看板（Board）**:
我的空间中的任务面板，由五个固定列组成：待规划、待办、进行中、待验收、已完成。列不可增删、改名或排序。
_Avoid_: 面板、白板

**验收清单（DoD，Definition of Done）**:
挂在任务上的人工验收清单，每项可附证据说明；清单勾满是成员验收的依据。
_Avoid_: 检查清单、subtask

**验收（Accept）**:
成员确认任务完成、将其移入「已完成」的动作。只有成员能验收，Agent 不能。
_Avoid_: 关闭、complete
